package utility;

import java.util.Random;

// This class to generate a strong password
public class PasswordGenerator {
  public static void main(String[] args) {
    System.out.println("Enter the length of the password to generate");
    //int size = HelperUtil.getInt();
    Random rand = new Random();
    // 1. Create a password with 8 letter
    // 2. Create a password with 16 letter
    // 3. Create a custom password
    // 4. Check password is strong
    // 5. Give suggesting to strengthen password
    // encode and decode your password
    // encrypt and decrypt the password
  }

}
